#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int ac, char **av)
{
        void radiation_pattern_driver_main( int ac, char **av );
        radiation_pattern_driver_main(ac,av);
        exit(0);
}
